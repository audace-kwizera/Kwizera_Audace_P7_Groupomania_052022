Backend => Groupomania

Serveur Node 
    install Nodemon
    start ==> nodemon server

Application Express 
    Middleware CORS installed

BD MySql
    Use ORM Sequelize (db:migrate)
    models => User & Message